# Django-Authetication System

This is a Django project that provides authentication and authorization features for a web application.

## Features
* User registration
* User login
* User logout
* Password reset
* Protected pages for authenticated users